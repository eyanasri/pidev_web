{% extends 'frontendconnected/base.html.twig' %}

{% block title %}Offre index{% endblock %}

{% block content %}
    {% if is_granted('ROLE_RECRUTEUR') %}
    <h1>Offre index</h1>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <h4 class="card-title ">Offre d'emploi</h4>
                        <p class="card-category"> Table</p>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <form action="#" method="get">
                                <div class="input-group">
                                    <!-- USE TWITTER TYPEAHEAD JSON WITH API TO SEARCH -->
                                    <input class="form-control" id="system-search" name="q" placeholder="Search for" required>
                                    <span class="input-group-btn">
                                          <button type="submit" class="btn btn-default"><i class="glyphicon glyphicon-search"></i></button>
                                            </span>
                                </div>
                            </form>
                            <table class="table  table-list-search">
                                <thead class=" text-primary">
                                <tr>
                                    <th>Id</th>
                                    <th>NomEntreprise</th>
                                    <th>Salaire</th>
                                    <th>Description</th>
                                    <th>Localisation</th>
                                    <th>NombreHeure</th>
                                    <th>TypeContrat</th>
                                    <th>NiveauEtude</th>
                                    <th>Experience</th>
                                    <th>Langue</th>
                                    <th>DateCreation</th>
                                    <th>Tel</th>
                                    <th>Email</th>
                                    <th>actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for offre in offres %}
                                    <tr>
                                        <td>{{ offre.id }}</td>
                                        <td>{{ offre.NomEntreprise }}</td>
                                        <td>{{ offre.Salaire }}</td>
                                        <td>{{ offre.Description }}</td>
                                        <td>{{ offre.Localisation }}</td>
                                        <td>{{ offre.NombreHeure }}</td>
                                        <td>{{ offre.TypeContrat }}</td>
                                        <td>{{ offre.NiveauEtude }}</td>
                                        <td>{{ offre.Experience }}</td>
                                        <td>{{ offre.Langue }}</td>
                                        <td>{{ offre.DateExpiration ? offre.DateExpiration|date('Y-m-d') : '' }}</td>
                                        <td>{{ offre.Tel }}</td>
                                        <td>{{ offre.Email }}</td>
                                        <td><td class="hidden-xs center">
                                            <a href="{{ path('recruteur_show', {'id': offre.id}) }}"><button class="btn btn-warning">show</button></a>
                                        </td>
                                        <td class="hidden-xs center">
                                            <a href="{{ path('recruteur_edit', {'id': offre.id}) }}"><button class="btn btn-warning">edit</button></a>
                                        </td>
                                        <td class="hidden-xs center">
                                            <a href="{{ path('recruteur_delete', {'id': offre.id}) }}"><button class="btn btn-warning">delete</button></a>
                                        </td>
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="14">no records found</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                            <td class="hidden-xs center">
                                <a href="{{ path('recruteur_new') }}"><button class="btn btn-warning">Create new</button></a>
                            </td>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% else %}
    <h1>Cannot acces this page</h1>
    {% endif %}
{% endblock %}