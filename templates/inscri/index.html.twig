<!DOCTYPE html>

<title>Inscri index</title>

{% block body %}
    <h1>Inscri index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for inscri in inscris %}
            <tr>
                <td>{{ inscri.id }}</td>
                <td>
                    <a href="{{ path('inscri_show', {'id': inscri.id}) }}">show</a>
                    <a href="{{ path('inscri_edit', {'id': inscri.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('inscri_new') }}">Create new</a>
{% endblock %}
