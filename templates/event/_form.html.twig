{{ form_start(form) }}
{{ form_errors(form) }}
<div class="mt-10">
    {{ form_errors(form.nom) }}
    {{ form_widget(form.nom,{'attr':{'class':'single-input ','placeholder':'Nom'}}) }}

</div>
<div class="mt-10">
    {{ form_errors(form.lieu) }}
    {{ form_widget(form.lieu,{'attr':{'class':'single-input','placeholder':'Lieu'}}) }}

<div class="mt-10">
    {{ form_label(form.date)}}
    {{ form_errors(form.date) }}
    <table>

        <th>{{ form_widget(form.date.day) }}</th>
        <th>{{ form_widget(form.date.month) }}</th>
        <th> {{ form_widget(form.date.year) }}</th>
    </table>
</div>
<div class="mt-10">
    {{ form_errors(form.nom) }}
    {{ form_widget(form.capacite,{'attr':{'class':'single-input','placeholder':'Capacité'}}) }}</div>
<div class="mt-10">
    {{ form_label(form.complet) }}
    {{ form_errors(form.complet) }}
    {{ form_widget(form.complet) }}
</div>

<p>

</p>
     <button type="submit" href="{{path('event_new')}}" class="btn"/> Save</button>
{{ form_end(form) }}