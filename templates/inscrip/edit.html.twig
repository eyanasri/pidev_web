{% extends 'Frontend/base.html.twig' %}

{% block title %}<title>Edit Inscrip</title>{% endblock %}

{% block content %}
<div style="width:800px; margin:0 auto;">
    <div class="vertical-space-100"></div>
    <div class="vertical-space-100"></div>
    <h1>Modifier l'Inscription</h1>

    {{ include('inscrip/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('inscrip_index') }}"><button class="btn btn-warning">Retour Vers la Liste</button></a>

    {{ include('inscrip/_delete_form.html.twig') }}
</div>
{% endblock %}
